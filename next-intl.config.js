// next-intl runtime configuration (JS only; TS not supported)
// See: https://next-intl.dev/docs/getting-started/app-router
const config = {
  locales: ['zh', 'en'],
  defaultLocale: 'zh'
};

export default config;
